# URL of the Next.js backend (use your machine IP for physical devices)
EXPO_PUBLIC_API_URL=http://localhost:3000

# Optional: NextAuth session cookie for development on native devices
# Copy from browser DevTools after signing in on the web app
# EXPO_PUBLIC_SESSION_COOKIE=next-auth.session-token=...
